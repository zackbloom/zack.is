dependencies:
  cache_directories:
    - "cache"

  post:
    - curl https://s3.amazonaws.com/stout-builds/install | sh

test:
  override:
    - echo "No tests!"

deployment:
  production:
    branch: master
    commands:
      - stout deploy --env production --key $AWS_KEY --secret $AWS_SECRET
